type logs struct{

}
type Event{
	timestamp 
	s
}
func findMax(logs)int{
	var envents []int
	for _,log:=range logs{
		envents=append(evnet[log.login_ts],1)
		envents=append(evnet[log.logout_ts],-1)
	}
	sort.Slice(event)
	maxCount:=0
	count:=0
	for _,envnt:=range evnents{
		
	prevcount:=count
	count+=event.s
	if count>maxCount{
		maxCount=count

	}
	}
	
}